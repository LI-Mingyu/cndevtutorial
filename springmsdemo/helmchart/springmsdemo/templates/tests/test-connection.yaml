apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springmsdemo.fullname" . }}-test-connection"
  labels:
    {{- include "springmsdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springmsdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
